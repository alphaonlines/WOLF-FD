# Copy this file to .env (or create a docker compose override)
# Frontend build-time API base URL
# Use /fd/api when running behind the nginx route in compose
VITE_POS_API_BASE_URL=/fd/api

# Backend + database settings
PGHOST=postgres
PGPORT=5432
PGDATABASE=salesdb
PGUSER=salesapp
PGPASSWORD=change_me_immediately
PORT=5057
NODE_ENV=production
