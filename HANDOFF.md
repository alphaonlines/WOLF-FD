# Archived Handoff Pointer

`AGENTS.md` is now the canonical WOLF-FD agent handoff.

The former BotBot shift-handoff content was folded into the `Active Work Queue` and monthly archive links in `AGENTS.md`. Future agents should update `AGENTS.md` after meaningful work instead of adding new entries here.
