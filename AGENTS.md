# Repository Guidelines

## Purpose

This file is the shared working memory for `/home/alphahs/WOLF-FD`. Keep it current, concise, and operational so the next work session can resume quickly.

## Logging Rules

- Append a new Running Log entry for every meaningful task.
- Use local timestamps in `YYYY-MM-DD HH:MM TZ` format.
- Keep entries factual and compact. Do not record secrets, tokens, or raw passwords.
- Include what changed, where, and what commands/tests were run, or note `not run`.
- If the log starts getting noisy, condense older history and rely on `git log --follow -- AGENTS.md` for full detail.
- Commit discipline: when a requested major repo change is complete, create a commit unless the user explicitly says not to.

## Current Stack

- Frontend: Vite + React + TypeScript, built from the repo root into `dist/`.
- Backend: Node.js + Express + TypeScript in `pos-dashboard-backend/`.
- Process manager: PM2 process `pos-api`, currently started from `/home/alphahs/WOLF-FD/pos-dashboard-backend` with `npm run start`.
- Database: PostgreSQL via backend `PG*` environment variables.
- Auth: HTTP-only session cookies plus Google Workspace sign-in/request-access flow.
- Live URL: `https://furnituredistributors.wolf.discount/fd/`
- Live API path: `https://furnituredistributors.wolf.discount/fd/api/`
- Current branch: `main`
- Current display version in `package.json`: `0.3.28.15`

## Project Structure

- `App.tsx`, `index.tsx`, `index.html`: app shell and entrypoints.
- `components/`: main workspaces and UI modules.
- Key workspaces:
  - `components/CRMWorkspace.tsx`
  - `components/SalesDashboard.tsx`
  - `components/ProductSearchWorkspace.tsx`
  - `components/ManufacturerPricelistPortal.tsx`
  - `components/UpdateDatabase.tsx`
  - `components/OwnerSettings.tsx`
  - `components/AdminUsers.tsx`
  - `components/MessageBoard.tsx`
- `components/app/`: shell, tabs, auth screen, theme, permissions.
- `components/sales/`: Sales Analysis print/report subcomponents.
- `services/`: frontend API clients and persistence helpers.
- `types.ts`, `constants.ts`: shared frontend contracts.
- `pos-dashboard-backend/src/`: backend runtime and route wiring.
- `pos-dashboard-backend/src/routes/`: feature route modules for auth, CRM, board, sales, tasks, manufacturer pricebooks, and system endpoints.
- `pos-dashboard-backend/db/schema.sql`: database schema baseline.
- `pos-dashboard-backend/importer/`: POS import scripts.
- `pos-dashboard-backend/manufacturer-pricebooks/holding/`: staged manufacturer uploads before preview/publish.
- `dist/`: built frontend output. Treat it as deploy artifact, not source of truth.

## Build, Run, and Deploy

From repo root:

- `npm install`
- `npm run dev`
- `npm run build`

From backend:

- `cd /home/alphahs/WOLF-FD/pos-dashboard-backend && npm install`
- `cd /home/alphahs/WOLF-FD/pos-dashboard-backend && npm run dev`
- `cd /home/alphahs/WOLF-FD/pos-dashboard-backend && npm run build`
- `cd /home/alphahs/WOLF-FD/pos-dashboard-backend && npm run start`

Operational checks:

- `pm2 describe pos-api`
- `pm2 restart pos-api`
- `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`
- `curl -skS -o /tmp/auth-config.out -w '%{http_code}' -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/api/auth/config`

Frontend deploy flow:

1. `cd /home/alphahs/WOLF-FD && npm run build`
2. `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`
3. Hard refresh the browser after deploy.

Backend deploy flow for route/schema/runtime changes:

1. `cd /home/alphahs/WOLF-FD/pos-dashboard-backend && npm run build`
2. `pm2 restart pos-api`
3. Verify through the live nginx path, not just direct localhost assumptions.

## Environment Notes

- Backend default runtime port is `5057` (`pos-dashboard-backend/src/runtimeConfig.ts`).
- Backend default DB fallbacks are `127.0.0.1:5432 / salesdb / salesapp`, but production values may be overridden by `.env`.
- Frontend production traffic goes through nginx at `/fd/` and `/fd/api/`.
- Frontend services intentionally hit `/fd/api/api/...` for the backend’s Express route layout. Do not “simplify” that path without checking the live proxy behavior.
- Manufacturer catalog work is now a first-class workflow:
  - staging uploads
  - preview/publish by manufacturer
  - shared catalog search
  - dedicated `Product Search` workspace
- Do not commit secrets from `.env`, auth config, DB credentials, or uploaded private vendor files.

## Coding and Maintenance Conventions

- TypeScript + React throughout the frontend; TypeScript + Express in the backend.
- Use 2-space indentation and follow surrounding file style.
- `PascalCase` for components, `camelCase` for functions/variables, `SCREAMING_SNAKE_CASE` for constants.
- Prefer small focused route modules and typed service APIs.
- Bump `displayVersion` in `package.json` when shipping visible live UI changes.
- Do not hand-edit deployed assets under `/srv/www/...`; always rebuild and sync from `dist/`.
- Keep this file useful: summarize patterns, current topology, and meaningful milestones rather than dumping every exploratory command forever.

## Working Snapshot

- Current active areas: CRM queue/workflow polish, Sales Analysis reporting, employee permissions, manufacturer pricebook ingest/publish, and Product Search.
- The backend route surface is now modularized under `pos-dashboard-backend/src/routes/`.
- Older pre-cleanup AGENTS detail was condensed on `2026-03-28 23:14 EDT`; use `git log --follow -- AGENTS.md` if you need the full historical trail.

## Running Log

- 2026-03-28 17:58 EDT — Added the new manufacturer price-book portal entry inside `Update Database` and shipped the first large frontend portal pass for upload, validation, correction, and search. Files: `App.tsx`, `components/UpdateDatabase.tsx`, `components/ManufacturerPricelistPortal.tsx`, `MANUFACTURER_PRICELIST_TODO.md`, `package.json`. Commands: frontend `npm run build`, deploy copy to `/srv/www/wolf.discount/fd/`, live bundle string checks. Tests: frontend build PASS; live bundle sync PASS.
- 2026-03-28 18:11 EDT — Added the real holding-upload flow for manufacturer files, owner-only upload/list APIs, upload metadata storage, and the frontend `Upload to Holding Folder` path. Files: `.gitignore`, `components/ManufacturerPricelistPortal.tsx`, `services/manufacturerPricelistApi.ts`, `types.ts`, `pos-dashboard-backend/db/schema.sql`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`, `pos-dashboard-backend/src/startupBootstrap.ts`, `package.json`. Commands: frontend/backend `npm run build`, `pm2 restart pos-api`, deploy copy, authenticated API checks. Tests: frontend build PASS; backend build PASS; backend route verification PASS.
- 2026-03-28 18:41 EDT — Built the first live Liberty manufacturer pipeline from the uploaded PDF and expanded the portal for multi-file and ZIP staging plus richer search/notes support. Files: `components/ManufacturerPricelistPortal.tsx`, `services/manufacturerPricelistApi.ts`, `types.ts`, `pos-dashboard-backend/src/parsers/libertyPricebook.ts`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`, `pos-dashboard-backend/src/startupBootstrap.ts`, `package.json`. Commands: `pdfinfo`, `pdftotext -layout`, frontend/backend `npm run build`, `pm2 restart pos-api`, authenticated preview/publish/catalog checks. Tests: frontend build PASS; backend build PASS; Liberty preview PASS; Liberty publish PASS.
- 2026-03-28 18:54 EDT — Added ZIP auto-unpack for manufacturer uploads so archive children are extracted into holding, linked by `parent_upload_id`, and surfaced in the portal. Files: `components/ManufacturerPricelistPortal.tsx`, `services/manufacturerPricelistApi.ts`, `types.ts`, `pos-dashboard-backend/db/schema.sql`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`, `pos-dashboard-backend/src/startupBootstrap.ts`, `package.json`. Commands: frontend/backend `npm run build`, `pm2 restart pos-api`, ZIP smoke upload, direct archive unpack for existing `Best` upload. Tests: frontend build PASS; backend build PASS; ZIP upload smoke PASS.
- 2026-03-28 19:14 EDT — Added the Best workbook parser and published the first Best catalog from the staged residential workbook. Files: `components/ManufacturerPricelistPortal.tsx`, `MANUFACTURER_PRICELIST_TODO.md`, `package.json`, `pos-dashboard-backend/src/parsers/bestPricebook.ts`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`, `pos-dashboard-backend/package.json`, `pos-dashboard-backend/package-lock.json`. Commands: workbook inspection with `node` + `xlsx`, backend/frontend builds, deploy copy, `pm2 restart pos-api`, authenticated preview/publish/catalog checks. Tests: frontend build PASS; backend build PASS; Best preview PASS; Best publish PASS.
- 2026-03-28 20:29 EDT — Improved light-mode readability in the CRM queue and committed the fix as `dc05022` (`fix: improve crm queue contrast in light mode`). Files: `components/CRMWorkspace.tsx`, `package.json`. Commands: `git diff`, `npm run build`, deploy copy, `git add`, `git commit`. Tests: frontend build PASS; live bundle sync PASS.
- 2026-03-28 20:34 EDT — Added the dedicated `Product Search` workspace to the app shell, wired permission support for `module.product_search`, and committed it as `4ee23b9` (`feat: add dedicated product search workspace`). Files: `components/ProductSearchWorkspace.tsx`, `components/app/tabs.ts`, `components/app/permissions.ts`, `App.tsx`, `package.json`, `pos-dashboard-backend/src/permissionCatalog.ts`. Commands: frontend/backend builds, deploy copy, `pm2 restart pos-api`, health check, `git add`, `git commit`. Tests: frontend build PASS; backend build PASS; backend health PASS.
- 2026-03-28 20:42 EDT — Backfilled `module.product_search` for older explicit-permission Owner accounts and updated owner fallback permission logic so new owner-default modules stay visible. Files: `components/app/permissions.ts`, `package.json`. Commands: Postgres permission backfill query, verification query, frontend `npm run build`, deploy copy, backend health check. Tests: DB backfill PASS; frontend build PASS; backend health PASS.
- 2026-03-28 20:51 EDT — Tightened manufacturer ingest behavior so publish stays manufacturer-scoped and upload type detection can default automatically. Files: `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`, `components/ManufacturerPricelistPortal.tsx`, `package.json`. Commands: frontend/backend builds, deploy copy, `pm2 restart pos-api`, health check, manufacturer coexistence query. Tests: frontend build PASS; backend build PASS; backend health PASS; catalog coexistence PASS.
- 2026-03-28 20:56 EDT — Updated `pro1st` handling to exclude mattress and box spring rows everywhere the dashboard calculates Pro1st, added `Pro1st` sales to Sales Analysis, and committed the change as `063faa3` (`feat: add filtered pro1st sales to sales analysis`). Files: `components/sales/SalesReportCard.tsx`, `components/salesReportUtils.ts`, `services/posBackendApi.ts`, `pos-dashboard-backend/src/pro1stSql.ts`, `pos-dashboard-backend/src/routes/reportRoutes.ts`, `pos-dashboard-backend/src/routes/salesDetailRoutes.ts`, `pos-dashboard-backend/src/routes/itemProRoutes.ts`, `pos-dashboard-backend/importer/import_pos_xlsx.py`. Commands: frontend/backend builds, deploy copy, `pm2 restart pos-api`, health probe, auth-guarded report probe, `git add`, `git commit`. Tests: backend build PASS; frontend build PASS; backend health PASS.
- 2026-03-28 20:57 EDT — Removed the `160`-row cap from the dedicated Product Search workspace, increased the fetch limit to `5000`, and shipped display version `0.3.28.15`. Files: `components/ProductSearchWorkspace.tsx`, `package.json`. Commands: frontend `npm run build`, deploy copy, live bundle grep, manufacturer-count query. Tests: frontend build PASS; live bundle sync PASS; DB manufacturer counts PASS.
- 2026-03-28 23:14 EDT — Cleaned up `AGENTS.md` to reflect the current WOLF-FD stack and workflow. Removed stale Firebase/general-server guidance, rewrote the repo overview around the actual frontend/backend/PM2/nginx setup, condensed the running log to recent actionable history, and kept older detail recoverable through git history instead of leaving the file bloated. Files: `AGENTS.md`. Commands: `sed`, `find`, `pm2 describe pos-api`, `ss -ltnup`, live nginx health probe, `git log --oneline`, `date`. Tests: doc-only change; live health probe via nginx PASS (`{"ok":true,"db":1}`).
- 2026-03-28 23:16 EDT — Committed the WOLF-FD AGENTS cleanup as `db2d757` (`docs: streamline repo guidance`). Files: `AGENTS.md`. Commands: `git -C /home/alphahs/WOLF-FD add AGENTS.md`, `git -C /home/alphahs/WOLF-FD commit -m "docs: streamline repo guidance"`. Tests: not run after commit; prior live health probe remained PASS.
- 2026-03-28 23:30 EDT — Updated the CRM opportunity queue to support multiple active customers per salesperson instead of a single shared `current_customer` slot. Added `crm_ups_active_customers` storage plus bootstrap/schema migration, migrated existing live working rows into active-customer records, updated CRM queue APIs to return aggregated active customer lists per rep and added per-customer update/complete/remove endpoints, then rewired `components/CRMWorkspace.tsx` and `services/crmApi.ts` so the selected rep card shows a stack of active customers with per-customer load/sync/complete/remove actions. Files: `components/CRMWorkspace.tsx`, `services/crmApi.ts`, `types.ts`, `pos-dashboard-backend/src/routes/crmRoutesV2.ts`, `pos-dashboard-backend/src/startupBootstrap.ts`, `pos-dashboard-backend/db/schema.sql`. Commands: frontend `npm run build`, backend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; backend build PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-28 23:35 EDT — Exposed the multi-customer CRM flow in the live UI by letting reps who are already `With Customer` add another active customer directly from the selected queue card. Also removed the `Sync Opportunity Card` and `Load Into Panel` buttons to simplify the workflow; active customer cards are now directly selectable instead. Files: `components/CRMWorkspace.tsx`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-28 23:42 EDT — Simplified the CRM customer panel by replacing the separate `Save Lead` and `Save Account` actions with one `Save Customer` button. The unified save now updates the customer account first, updates or creates the lead automatically when a phone number is present, and syncs the selected active queue customer behind the scenes when the panel is tied to a live opportunity. Files: `components/CRMWorkspace.tsx`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-28 23:44 EDT — Bumped the visible WOLF-FD app version from `0.3.28.15` to `0.3.28.16` in `package.json`, rebuilt the frontend, redeployed the live bundle, and rechecked the FD health path. Files: `package.json`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-28 23:56 EDT — Aligned the CRM save flow with the showroom workflow so `Complete Up` is now the UPS-history save path and `Save Customer` only updates the customer account. Updated `components/CRMWorkspace.tsx` so completing a loaded active customer first syncs the current panel name/description back into the active UPS record, then completes it into history; `Save Customer` no longer creates or updates CRM leads and now only upserts the customer account plus the linked active queue customer details when present. Files: `components/CRMWorkspace.tsx`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-28 23:57 EDT — Committed the CRM save-flow alignment as `baf3a21` (`fix: align crm save flow with ups history`). Files: `AGENTS.md`, `components/CRMWorkspace.tsx`. Commands: `git -C /home/alphahs/WOLF-FD add AGENTS.md components/CRMWorkspace.tsx`, `git -C /home/alphahs/WOLF-FD commit -m "fix: align crm save flow with ups history"`, `date`. Tests: not run after commit; prior frontend build and live health check remained PASS.
- 2026-03-29 00:05 EDT — Fixed a regression in the CRM `Complete` button so active ups complete reliably again even when the panel has a blank customer name. Updated `components/CRMWorkspace.tsx` to only pre-sync valid changed fields before completion, skip the blocking customer-name patch when blank, and set/reset the queue saving state around the complete flow. Files: `components/CRMWorkspace.tsx`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; live bundle updated to `assets/index-CoXQItb8.js`; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 00:09 EDT — Fixed the CRM customer-name editor so typing in the panel no longer gets reverted by the 4-second UPS queue poll. Updated `components/CRMWorkspace.tsx` so the draft only hydrates from the selected active customer when switching to a different queue customer, instead of reloading the same active record on every refreshed queue payload. Files: `components/CRMWorkspace.tsx`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; live bundle updated to `assets/index-BQJ7xlnO.js`; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 00:33 EDT — Fixed CRM queue ownership so employees can manage and complete rows that were checked in without `rep_user_id`. Updated `pos-dashboard-backend/src/routes/crmRoutesV2.ts` to authorize sales users by matched rep name when a queue row has no linked user id, and updated `components/CRMWorkspace.tsx` so the page also treats same-name manual rows as the logged-in user’s row for selection and button enablement. Rebuilt frontend + backend, redeployed the frontend bundle, restarted `pos-api`, and verified the live FD health path plus live queue ownership (`Anthony Calkins` row now attached to `rep_user_id=3`). Files: `components/CRMWorkspace.tsx`, `pos-dashboard-backend/src/routes/crmRoutesV2.ts`. Commands: frontend `npm run build`, backend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, escalated Node+pg inspection queries against `crm_ups_queue`, `crm_ups_active_customers`, and `users`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; backend build PASS; PM2 restart PASS; live health PASS (`{"ok":true,"db":1}`); live queue ownership verification PASS.
- 2026-03-29 00:42 EDT — Switched the CRM ups queue to a shared-management model for all signed-in users after the row-ownership fallback still wasn’t resolving live `Complete` failures. Updated `pos-dashboard-backend/src/routes/crmRoutesV2.ts` so any authenticated user can manage queue rows, updated `components/CRMWorkspace.tsx` so `Complete`/`Remove Up`/status controls are always enabled on visible rows, and added visible success/error banners because `statusMessage` and `errorMessage` had been hidden from the UI. Rebuilt frontend + backend, redeployed the frontend bundle, restarted `pos-api`, and verified the live FD health path. Files: `components/CRMWorkspace.tsx`, `pos-dashboard-backend/src/routes/crmRoutesV2.ts`. Commands: frontend `npm run build`, backend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, source greps for `canManageUpsQueueRow`, `canManageRow = true`, and status/error banners, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; backend build PASS; PM2 restart PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 00:43 EDT — Fixed the backend 500 that still blocked `Complete` after the queue-action changes. Root cause: the `/api/crm/ups-queue/:id/customers/:customerId` patch route was updating `crm_ups_active_customers` with non-existent queue column names (`current_customer`, `current_customer_type`, `current_customer_details`) instead of the table’s actual columns (`customer`, `customer_type`, `customer_details`). Updated `pos-dashboard-backend/src/routes/crmRoutesV2.ts`, rebuilt the backend, restarted `pos-api`, and rechecked the live FD health path. Files: `pos-dashboard-backend/src/routes/crmRoutesV2.ts`. Commands: backend `npm run build`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: backend build PASS; PM2 restart PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 00:51 EDT — Bumped the visible WOLF-FD frontend version from `0.3.28` to `0.3.29` in `package.json`, rebuilt the frontend, redeployed the live bundle, and rechecked the FD health path. Files: `package.json`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 00:54 EDT — Updated the CRM `Save Customer` flow so it also refreshes the live ups queue card with the current customer-panel info without completing or removing the up. `components/CRMWorkspace.tsx` now syncs the active up row first using the panel name plus combined visual-description/notes text, keeps the up active in the queue, and if phone/email is still missing it shows `Up list updated...` instead of failing the whole button action. Files: `components/CRMWorkspace.tsx`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `grep` verification for new status text, `date`. Tests: frontend build PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 00:03 EDT — Tightened the shared `pro1st` exclusions so Sales Analysis and related Pro1st reporting no longer count mattresses, box springs, foundations, adjustable bases, power bases, or bunkie boards. Updated `pos-dashboard-backend/src/pro1stSql.ts` and the import-time `is_pro1st` filter in `pos-dashboard-backend/importer/import_pos_xlsx.py`, rebuilt the backend, restarted `pos-api`, and verified the live FD API health endpoint. Commands: backend `npm run build`, `pm2 restart pos-api`, `grep -RIn "foundation\\|adjustable base\\|bunkie board" /home/alphahs/WOLF-FD/pos-dashboard-backend/dist`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: backend build PASS; dist grep PASS; live health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 00:04 EDT — Committed the Pro1st bedding-foundation exclusion fix as `badb8ed` (`fix: exclude bedding foundations from pro1st`). Files: `AGENTS.md`, `pos-dashboard-backend/src/pro1stSql.ts`, `pos-dashboard-backend/importer/import_pos_xlsx.py`. Commands: `git -C /home/alphahs/WOLF-FD add AGENTS.md pos-dashboard-backend/src/pro1stSql.ts pos-dashboard-backend/importer/import_pos_xlsx.py`, `git -C /home/alphahs/WOLF-FD commit -m "fix: exclude bedding foundations from pro1st"`, `date`. Tests: not run after commit; prior backend build and live health check remained PASS.
- 2026-03-29 00:06 EDT — Changed the Sales Analysis `Pro1st` column to show percent of sale instead of raw dollars by calculating `pro1st_sales / total_retail * 100` for each row and the totals footer in `components/sales/SalesReportCard.tsx`. Rebuilt the frontend, redeployed `/srv/www/wolf.discount/fd/`, and verified the live bundle contains the new `Pro1st % of Sale` label. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `grep -RIn "Pro1st % of Sale\\|foundation-related lines" /srv/www/wolf.discount/fd/index.html /srv/www/wolf.discount/fd/assets`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS; live bundle string check PASS.
- 2026-03-29 00:07 EDT — Committed the Sales Analysis Pro1st percent-of-sale fix as `6424b73` (`fix: show pro1st as percent of sale`). Files: `AGENTS.md`, `components/sales/SalesReportCard.tsx`. Commands: `git -C /home/alphahs/WOLF-FD add AGENTS.md components/sales/SalesReportCard.tsx`, `git -C /home/alphahs/WOLF-FD commit -m "fix: show pro1st as percent of sale"`, `git -C /home/alphahs/WOLF-FD status --short --branch`, `date`. Tests: not run after commit; prior frontend build and live bundle check remained PASS.
- 2026-03-29 00:20 EDT — Consolidated the Sales Dashboard top stat cards into fewer toggle-aware summary cards in `components/SalesDashboard.tsx`. Replaced the separate transactions/total-sales/average-ticket/financed-amount/financed-transactions cards with combined `Sales Overview` and `Finance Overview` cards, made their primary metric follow the header toggle (`Sales` shows dollars first; `QTY` shows transaction counts first), normalized saved card-order preferences to collapse legacy layouts into the new three-card row, rebuilt the frontend, redeployed `/srv/www/wolf.discount/fd/`, and verified the live bundle contains the new overview labels/tooltips. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `grep -RIn "Sales Overview\\|Finance Overview\\|QTY mode is showing transaction count first\\|The main number follows the header toggle" /srv/www/wolf.discount/fd/index.html /srv/www/wolf.discount/fd/assets`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS; live bundle string check PASS.
- 2026-03-29 00:20 EDT — Committed the Sales Dashboard summary-card consolidation as `0dea07d` (`feat: consolidate sales dashboard summary cards`). Files: `AGENTS.md`, `components/SalesDashboard.tsx`. Commands: `git -C /home/alphahs/WOLF-FD add components/SalesDashboard.tsx AGENTS.md`, `git -C /home/alphahs/WOLF-FD commit -m "feat: consolidate sales dashboard summary cards"`, `date`. Tests: not run after commit; prior frontend build and live bundle check remained PASS.
- 2026-03-29 00:23 EDT — Added more vertical breathing room below the Sales Dashboard top summary-card row so the next card section no longer visually touches it. Updated `components/SalesDashboard.tsx` to wrap the draggable summary-card grid with an explicit bottom margin, rebuilt the frontend, and redeployed `/srv/www/wolf.discount/fd/` with live bundle `assets/index-CurUo5WO.js`. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS.
- 2026-03-29 00:27 EDT — Fixed the lower Sales Dashboard card stack spacing so the bottom sections no longer sit flush against each other. Updated `components/SalesDashboard.tsx` to give the main on-screen `fd-print-hide` wrapper a shared `space-y-6` vertical rhythm, rebuilt the frontend, and redeployed `/srv/www/wolf.discount/fd/` with live bundle `assets/index-Cue-rawL.js`. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS.
- 2026-03-29 00:39 EDT — Updated the Sales Report table to follow the header sort intent more closely. In `components/SalesDashboard.tsx`, report rows and print rows now sort by total retail in `Sales` mode and by ticket count in `QTY` mode (with units and retail as tie-breakers). In `components/sales/SalesReportCard.tsx`, added a visible `Tickets` column and an inline note explaining that `QTY` mode ranks by tickets for the selected range. Rebuilt the frontend and redeployed `/srv/www/wolf.discount/fd/` with live bundle `assets/index-CM7rQvAd.js`. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS.
- 2026-03-29 00:40 EDT — Committed the Sales Report sort update as `05c7430` (`fix: sort sales report by tickets in qty mode`). Files: `AGENTS.md`, `components/SalesDashboard.tsx`, `components/sales/SalesReportCard.tsx`. Commands: `git -C /home/alphahs/WOLF-FD add components/SalesDashboard.tsx components/sales/SalesReportCard.tsx AGENTS.md`, `git -C /home/alphahs/WOLF-FD commit -m "fix: sort sales report by tickets in qty mode"`, `date`. Tests: not run after commit; prior frontend build and live redeploy remained PASS.
- 2026-03-29 00:51 EDT — Updated the WOLF-FD app version metadata. Changed `package.json` version to `0.3.29` and visible `displayVersion` to `0.3.29.1`, rebuilt the frontend, and redeployed `/srv/www/wolf.discount/fd/` with live bundle `assets/index-mbOarDHB.js`. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS.
- 2026-03-29 00:52 EDT — Committed the app version bump as `c7552d6` (`chore: bump app version to 0.3.29.1`). Files: `AGENTS.md`, `package.json`. Commands: `git -C /home/alphahs/WOLF-FD add package.json AGENTS.md`, `git -C /home/alphahs/WOLF-FD commit -m "chore: bump app version to 0.3.29.1"`, `git -C /home/alphahs/WOLF-FD log -1 --oneline`, `date`. Tests: not run after commit; prior frontend build and live redeploy remained PASS.
- 2026-03-29 00:24 EDT — Committed the Sales Dashboard spacing polish as `fdd31e0` (`fix: add spacing below sales summary cards`). Files: `AGENTS.md`, `components/SalesDashboard.tsx`. Commands: `git -C /home/alphahs/WOLF-FD add components/SalesDashboard.tsx AGENTS.md`, `git -C /home/alphahs/WOLF-FD commit -m "fix: add spacing below sales summary cards"`, `date`. Tests: not run after commit; prior frontend build and live redeploy remained PASS.
- 2026-03-29 01:07 EDT — Unified CRM leads and customer accounts into a single customer record flow. Files: `components/CRMWorkspace.tsx`, `services/crmApi.ts`, `types.ts`, `pos-dashboard-backend/src/routes/crmRoutesV2.ts`, `pos-dashboard-backend/src/startupBootstrap.ts`, `pos-dashboard-backend/db/schema.sql`. Changes: customer save/search now use `crm_customers` as the primary CRM record, lead metadata fields live on customer records, legacy CRM lead routes now read/write `crm_customers`, and bootstrap migrates existing `crm_leads` rows into `crm_customers` by normalized phone when possible. Commands: frontend `npm run build`, backend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; backend build PASS; FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 01:13 EDT — Added salesperson drilldown from the Sales Report table so clicking a salesperson row opens that associate’s ticket list below the charts. Files: `components/SalesDashboard.tsx`, `components/sales/SalesReportCard.tsx`. Changes: report rows in salesperson mode now render clickable names, the active selected row is highlighted, and selecting a row scrolls the existing `SalespersonDetailCard` into view. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS.
- 2026-03-29 01:20 EDT — Added Pro1st dollars and percent to the salesperson ticket drilldown. Files: `services/posBackendApi.ts`, `components/sales/SalespersonDetailCard.tsx`. Changes: frontend now maps `pro1st_sales` and `pro1st_pct` from `/api/salesperson-tickets` and displays them in the salesperson detail table alongside total, profit, and margin. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS.
- 2026-03-29 01:53 EDT — Removed the visible import log from the manufacturer upload area. File: `components/ManufacturerPricelistPortal.tsx`. Changes: deleted the right-rail `Holding uploads` display block from the upload/ingestion screen while leaving the underlying holding-folder upload, auto-selection, preview, and publish logic intact. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS.
- 2026-03-29 01:57 EDT — Fixed Sales Report ticket counts so split tickets are only counted once for the first salesperson listed on the sale. File: `pos-dashboard-backend/src/routes/reportRoutes.ts`. Changes: `/api/report/sales-summary` now derives a `primary_salesperson` per sale from the raw `pos_sales.salesperson` string and uses that to compute salesperson `ticket_count`, while keeping split sales/profit/Pro1st/unit values shared across all participants. Commands: backend `npm run build`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: backend build PASS; FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 02:02 EDT — Reverted the salesperson row ticket-count change and moved the split-ticket fix to the bottom totals only. Files: `pos-dashboard-backend/src/routes/reportRoutes.ts`, `services/posBackendApi.ts`, `components/SalesDashboard.tsx`, `components/salesReportUtils.ts`. Changes: salesperson rows in Sales Report now keep their original split ticket counts, while the backend returns a separate `distinctTicketCount` for the filtered report set and the bottom totals row uses that unique sale count instead of summing split rows. Commands: frontend `npm run build`, backend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: frontend build PASS; backend build PASS; FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 02:39 EDT — Fixed the Product Search manufacturer menu so the dedicated search workspace always exposes the canonical manufacturer list, including `England`, instead of only showing manufacturers present in the current result slice. Added shared frontend manufacturer config in `constants/productCatalog.ts`, updated `components/ProductSearchWorkspace.tsx` and `components/ManufacturerPricelistPortal.tsx` to use it, and raised the backend catalog route cap from `500` to `5000` in `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts` so the UI’s larger product fetch limit is honored. Deployed the rebuilt frontend to `/srv/www/wolf.discount/fd/` (live bundle `assets/index-CGVzJeiY.js`) and restarted `pos-api`. Commands: frontend `npm run build`, backend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS; backend build PASS; FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 02:45 EDT — Added first-pass England manufacturer support to the pricebook pipeline. Files: `pos-dashboard-backend/src/parsers/englandPricebook.ts`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`. Changes: wired England into manufacturer preview/publish parsing, added a streaming `pdftotext`-based parser for the held `England Price Book EAST 11-7-25.pdf`, and verified the live held PDF parses into 682 catalog rows without the old stdout buffer failure. Commands: backend `npm run build`, local parser smoke test via `node - <<'NODE'` against `/home/alphahs/WOLF-FD/pos-dashboard-backend/manufacturer-pricebooks/holding/england/1774766296003_drive-download-20260329T062647Z-3-001__unzipped_14/England Price Book EAST 11-7-25.pdf`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `date`. Tests: backend build PASS; England parser smoke test PASS (682 parsed rows); FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 02:49 EDT — Corrected England upload selection so extraction resolves to the real pricebook child PDF instead of the tariff/cushion sidecar files from the same ZIP. Files: `components/ManufacturerPricelistPortal.tsx`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`. Changes: updated both frontend and backend holding-upload scoring to reward `price book`/`pricebook` names and penalize tariff, schedule, covers, curfab/pattern, and cushion files; also made England child uploads with a `parent_upload_id` resolve through the preferred sibling selection path like Best. Deployed frontend bundle `assets/index-DsmDew7N.js` and restarted `pos-api`. Commands: frontend `npm run build`, backend `npm run build`, local selection-score smoke test via `node - <<'NODE'`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `date`. Tests: frontend build PASS; backend build PASS; score smoke test PASS (England pricebook ranked above tariff/cushion); FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 02:56 EDT — Simplified the manufacturer upload portal so it stays upload/validation-focused and hands catalog lookup off to the dedicated Product Search module. Files: `components/ManufacturerPricelistPortal.tsx`, `components/UpdateDatabase.tsx`, `App.tsx`, `package.json`. Changes: removed the embedded portal search screen, added validation-table sort controls (`Needs review first`, source order, manufacturer/category/item/description, price low/high), added `Open Full Product Search` actions in the portal header and validation toolbar, wired the update panel to open `Tab.PRODUCT_SEARCH` and close the panel cleanly, and bumped the visible app version to `0.3.29.10`. Deployed frontend bundle `assets/index-DGP9Ov0y.js`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `grep -RIn "0.3.29.10\\|Open Full Product Search\\|Needs review first" /srv/www/wolf.discount/fd/index.html /srv/www/wolf.discount/fd/assets`, `date`. Tests: frontend build PASS; live bundle string verification PASS.
- 2026-03-29 03:00 EDT — Added a real sort control to the dedicated Product Search workspace in `components/ProductSearchWorkspace.tsx`. Changes: added client-side sort options for relevance/extraction order, manufacturer, category, item #, and price low/high, and applied the selected order to the live catalog result cards. Deployed frontend bundle `assets/index-f5L36XGd.js`. Commands: frontend `npm run build`, `sudo cp -r /home/alphahs/WOLF-FD/dist/. /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 3`, `grep -RIn "Relevance / extraction order\\|Price low-high\\|Sort" /srv/www/wolf.discount/fd/index.html /srv/www/wolf.discount/fd/assets`, `date`. Tests: frontend build PASS; live bundle string verification PASS.
- 2026-03-29 03:15 EDT — Replaced the AlphaOS placeholder with a live per-system control board and removed the `Coming Soon` hero. File: `components/KiosksStatus.tsx`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-yl1TzLoC.js`. Changes: grouped AlphaOS systems by location, added `Remote In` on every system row, added per-system `Settings`/`Reboot`/`Update`/`Backup` actions inside the expanded detail panel, and surfaced uptime/version/IP/CPU/RAM/storage/backup stats plus a selected-system command banner. Commands: `sed -n '1,260p' /home/alphahs/WOLF-FD/components/KiosksStatus.tsx`, `grep -RIn "KiosksStatus\\|Alpha OS\\|AlphaOS\\|Kiosks" /home/alphahs/WOLF-FD --exclude-dir=node_modules`, `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -n "AlphaOS Command Board\\|Remote In\\|View stats\\|Backup" /srv/www/wolf.discount/fd/assets/index-yl1TzLoC.js | head -n 10`, `date '+%Y-%m-%d %H:%M %Z'`. Tests: frontend build PASS; live bundle string verification PASS.
- 2026-03-29 03:22 EDT — Merged CRM search into the customer panel so lookup now runs live as staff type into panel text fields, and clicking a salesperson row re-syncs the panel back to that up-line customer. File: `components/CRMWorkspace.tsx`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-BJyUcXdj.js`. Changes: removed the separate CRM search card, added debounced live customer/order lookup inside the customer panel keyed to the active typed field, capped the shown matches to the top 10 results, and made queue-row or active-customer clicks reload the panel from the selected active up instead of leaving the last searched customer in place. Commands: `grep -RIn "Customer Panel\\|customer search\\|crm search\\|findCrmCustomerAccount\\|searchCustomers\\|ups" /home/alphahs/WOLF-FD/components /home/alphahs/WOLF-FD/services /home/alphahs/WOLF-FD/types.ts --exclude-dir=node_modules`, `sed -n '1,360p' /home/alphahs/WOLF-FD/components/CRMWorkspace.tsx`, `sed -n '360,1280p' /home/alphahs/WOLF-FD/components/CRMWorkspace.tsx`, `sed -n '520,680p' /home/alphahs/WOLF-FD/services/crmApi.ts`, `sed -n '1860,1988p' /home/alphahs/WOLF-FD/pos-dashboard-backend/src/routes/crmRoutesV2.ts`, `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -n "Live matches\\|Searching First name\\|Customer Panel" /srv/www/wolf.discount/fd/assets/index-BJyUcXdj.js | head -n 10`, `date '+%Y-%m-%d %H:%M %Z'`. Tests: frontend build PASS; live bundle string verification PASS.
- 2026-03-29 03:31 EDT — Added an `Open Tickets` CRM card tied to the selected showroom and wired sale/items links into each row. Files: `components/CRMWorkspace.tsx`, `services/posBackendApi.ts`, `pos-dashboard-backend/src/routes/salesDetailRoutes.ts`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-CFlBQ6jg.js`. Changes: added backend route `GET /api/open-location-tickets` for store-mapped open sales, added frontend fetch helper and typed rows, and rendered a CRM right-rail card showing open tickets for the active store with `Sale #` link to the existing sales-record page plus `Items` link to `deliverieddetail.asp?saleid=...`. Store mapping used: `FD7 -> Morehead/Morehead City/Havelock`, `FD5 -> Newport`, `G1 -> Greenville`, `Base -> Cherry Point`, `Camp -> Camp LeJeune/Camp Lejeune`. Commands: `sed -n '1120,1188p' /home/alphahs/WOLF-FD/components/SalesDashboard.tsx`, `sed -n '1,260p' /home/alphahs/WOLF-FD/pos-dashboard-backend/src/routes/salesDetailRoutes.ts`, `node - <<'NODE'` SQL inspection of `pos_sales` columns/statuses/open-location counts from `/home/alphahs/WOLF-FD/pos-dashboard-backend`, frontend `npm run build`, backend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' 'https://127.0.0.1/fd/api/api/open-location-tickets?store=FD7&limit=5'`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -n "Open Tickets\\|Sale #\\|deliverieddetail.asp" /srv/www/wolf.discount/fd/assets/index-CFlBQ6jg.js | head -n 10`, `date '+%Y-%m-%d %H:%M %Z'`. Tests: frontend build PASS; backend build PASS; FD health PASS (`{"ok":true,"db":1}`); open-ticket endpoint probe returned `unauthorized` without CRM session as expected; live bundle string verification PASS.
- 2026-03-29 03:38 EDT — Simplified the manufacturer upload portal header and added `Jackson/Catnapper` as a selectable manufacturer option. Files: `components/ManufacturerPricelistPortal.tsx`, `constants/productCatalog.ts`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-CEkK5ngl.js`. Changes: removed the large top intro/brief/stats card from the portal so the screen opens straight into the upload/validation controls, added `Jackson/Catnapper` to the canonical manufacturer list used by upload/search filters, added a basic portal template for that manufacturer, and added sibling-resolution handling for `jackson-catnapper` child uploads selected from extracted archives. Commands: `grep -n "readyRowsCount\\|flaggedRows.length\\|const template\\b\\|const MANUFACTURERS\\b\\|MANUFACTURER_TEMPLATES" /home/alphahs/WOLF-FD/components/ManufacturerPricelistPortal.tsx`, `sed -n '520,620p' /home/alphahs/WOLF-FD/components/ManufacturerPricelistPortal.tsx`, `sed -n '790,890p' /home/alphahs/WOLF-FD/components/ManufacturerPricelistPortal.tsx`, `sed -n '80,220p' /home/alphahs/WOLF-FD/components/ManufacturerPricelistPortal.tsx`, frontend `npm run build`, backend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -skS -H 'Host: furnituredistributors.wolf.discount' https://127.0.0.1/fd/api/health`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -n "Jackson/Catnapper\\|Back to Update Database\\|Upload & Ingestion\\|Validation & Correction" /srv/www/wolf.discount/fd/assets/index-CEkK5ngl.js | head -n 20`, `date '+%Y-%m-%d %H:%M %Z'`. Tests: frontend build PASS; backend build PASS; FD health PASS (`{"ok":true,"db":1}`); live bundle string verification PASS. Note: no Jackson/Catnapper upload file or parser has been wired yet; this change makes the manufacturer selectable and archive child selection ready for the next upload.
- 2026-03-29 03:46 EDT — Added live Jackson/Catnapper backend parser support for the held pricebook PDF. Files: `pos-dashboard-backend/src/parsers/jacksonCatnapperPricebook.ts`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`. Changes: wired `jackson-catnapper` into preview/publish parsing and reference-note handling, added a streaming `pdftotext` parser for the held `1774769974385_Jackson FALL 2025 Kiosk Price List w-Apr Intros.pdf`, handled section/style/subtype state across fabric and leather pages, inherited repeated fabric/color values across grouped SKUs, skipped package-dimensions pages, and preserved wrapped descriptions like `Console Storage Box w/Bluetooth Sound System`. Commands: `pdftotext -layout ... | sed -n ...`, backend `npm run build` (multiple passes while tuning parser rules), local parser smoke tests via `node - <<'NODE'` against `/home/alphahs/WOLF-FD/pos-dashboard-backend/manufacturer-pricebooks/holding/jackson-catnapper/1774769974385_Jackson FALL 2025 Kiosk Price List w-Apr Intros.pdf`, `pm2 restart pos-api`, `curl -s http://127.0.0.1:5057/health`, `date`. Tests: backend build PASS; Jackson/Catnapper parser smoke test PASS (762 parsed rows); FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 03:55 EDT — Added direct SKU-based external item search links to the Product Search module. File: `components/ProductSearchWorkspace.tsx`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-DU0arn5J.js`. Changes: added a helper that builds `https://www.furnituredistributors.net/Product/SiteSearch?search=<SKU>` from each product SKU, surfaced an `Item Search: <SKU>` button on every catalog result card, and added a matching detail-panel action for the selected product. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -RIn "Item Search:\\|Open item number search\\|SiteSearch?search=" /srv/www/wolf.discount/fd/assets /srv/www/wolf.discount/fd/index.html | head -n 20`, `date`. Tests: frontend build PASS; live bundle string verification PASS.
- 2026-03-29 04:00 EDT — Audited the existing Best manufacturer import to see whether collection metadata and additional workbook fields were already being captured. Files inspected: `pos-dashboard-backend/src/parsers/bestPricebook.ts`, held workbook `pos-dashboard-backend/manufacturer-pricebooks/holding/best/1774738114590_drive-download-20260328T222647Z-3-001__unzipped_5/BEST October 23, 2025 Residential Price List Compressed.xls`. Findings: current import already populates `collection_name` and `collection_code` for most Best rows, but all 731 imported Best rows are missing dimensions, 4 barstool rows are missing collection metadata, and 2 rows have suspicious `color_finish` values (`CHAIR 2206`) caused by header parsing on special-cording layouts. Commands: `sed -n '1,520p' .../bestPricebook.ts`, workbook sheet inspection via `node - <<'NODE'` + `xlsx`, Postgres verification queries against `manufacturer_catalog_items` for `manufacturer_slug='best'`, `date`. Tests: inspection queries PASS; no code changes applied.
- 2026-03-29 04:23 EDT — Upgraded the Best manufacturer parser and republished the live Best catalog from upload `13`. Files: `pos-dashboard-backend/src/parsers/bestPricebook.ts`, `AGENTS.md`. Changes: added safe collection-name detection for names with digits, normalized odd cover labels like `CHAIR 2206` and `A 210`, backfilled missing collection codes from SKU families, captured overall dimensions from `HOW TO ORDER` blocks without changing the original 731-row count, rebuilt the backend, restarted `pos-api`, and republished Best against the held workbook `pos-dashboard-backend/manufacturer-pricebooks/holding/best/1774738114590_drive-download-20260328T222647Z-3-001__unzipped_5/BEST October 23, 2025 Residential Price List Compressed.xls`. Commands: backend parser smoke tests via `node -r ts-node/register/transpile-only`, backend `npm run build`, `pm2 restart pos-api`, `curl -s http://127.0.0.1:5057/health`, direct Postgres republish script via `node -r ts-node/register/transpile-only`, verification queries against `manufacturer_catalog_items` and `manufacturer_pricebook_uploads`, `git status`, `date`. Tests: parser smoke test PASS (`731` rows, `0` missing collection names/codes, `132` rows with dimensions, `0` `CHAIR 2206` values); backend build PASS; FD health PASS (`{"ok":true,"db":1}`); post-publish DB verification PASS.
- 2026-03-29 04:30 EDT — Added first-pass manufacturer support for the newly uploaded Guardsman, GBS ProtectAll, and Innovations ZIPs and remapped the existing holding uploads out of Ashley. Files: `pos-dashboard-backend/src/parsers/accessoryWorkbookPricebooks.ts`, `pos-dashboard-backend/src/routes/manufacturerPricebookRoutes.ts`, `constants/productCatalog.ts`, `components/ManufacturerPricelistPortal.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-06ZLSd-8.js`. Changes: added three new XLSX parsers (`Guardsman`, `GBS ProtectAll`, `Innovations`), extended backend preview/publish routing and sibling-child upload resolution for those manufacturers, improved holding-upload scoring to prefer `sku list` / `pricing by sku` workbooks and avoid order-form / kit files, added the new manufacturers to the canonical frontend list and upload templates, rebuilt/deployed the frontend, restarted `pos-api`, and updated upload rows `48-68` in `manufacturer_pricebook_uploads` so the already-extracted ZIPs now show under the correct manufacturer slugs (`guardsman`, `gbs-protectall`, `innovations`). Commands: workbook inspections via `node - <<'NODE'` + `xlsx`, frontend `npm run build`, backend `npm run build`, parser smoke tests via `node -r ts-node/register/transpile-only`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -s http://127.0.0.1:5057/health`, Postgres update/verification queries against `manufacturer_pricebook_uploads`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -RIn "Guardsman\\|GBS ProtectAll\\|Innovations" /srv/www/wolf.discount/fd/assets /srv/www/wolf.discount/fd/index.html | head -n 20`, `git status`, `date`. Tests: frontend build PASS; backend build PASS; parser smoke tests PASS (`Guardsman 41 rows`, `GBS ProtectAll 120 rows`, `Innovations 23 rows` from the SKU workbook); FD health PASS (`{"ok":true,"db":1}`); live bundle string verification PASS; holding-upload remap verification PASS.
- 2026-03-29 04:36 EDT — Bumped the visible WOLF-FD app version to `0.3.29.11`. Files: `package.json`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-rIRrJ_US.js`. Changes: updated `displayVersion` from `0.3.29.10` to `0.3.29.11`, rebuilt the frontend, redeployed the static bundle, and verified the new version string is present in the live asset. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -RIn "0.3.29.11" /srv/www/wolf.discount/fd/assets /srv/www/wolf.discount/fd/index.html | head -n 20`, `git status`, `date`. Tests: frontend build PASS; live bundle string verification PASS.
- 2026-03-29 04:40 EDT — Removed the visible `Tickets` column from the Sales Analysis totals tables to reduce horizontal scrolling while keeping ticket-based calculations for QTY ranking and average-ticket math. Files: `components/sales/SalesReportCard.tsx`, `components/sales/SalesCorePrintSections.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-hvbaXJN7.js`. Changes: deleted the `Tickets` header/value/footer cells from the live Sales Report totals table and from the print store/salesperson tables, rebuilt the frontend, redeployed the static bundle, and verified the updated bundle was published. Commands: `grep -n "Tickets\\|Average Ticket\\|QTY mode ranks the table by tickets" .../SalesReportCard.tsx .../SalesCorePrintSections.tsx`, frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -RIn "Average Ticket\\|Pro1st % of Sale\\|Sales % of View" /srv/www/wolf.discount/fd/assets /srv/www/wolf.discount/fd/index.html | head -n 20`, `date`. Tests: frontend build PASS; live asset deploy PASS.
- 2026-03-29 04:46 EDT — Compressed the Sales Analysis totals tables further so they fit the screen without horizontal scrolling by combining the three sales-percent columns into `Sales Mix` and the three units-percent columns into `Units Mix`. Files: `components/sales/SalesReportCard.tsx`, `components/sales/SalesCorePrintSections.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-ep1t4c84.js`. Changes: switched the live and print totals tables to `table-fixed`, tightened column widths, shortened `Average Ticket` to `Avg Ticket`, and collapsed the view/own/company percentage triplets into stacked summary cells. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -RIn "Sales Mix\\|Units Mix\\|Avg Ticket" /srv/www/wolf.discount/fd/assets /srv/www/wolf.discount/fd/index.html | head -n 20`, `date`. Tests: frontend build PASS; live bundle string verification PASS.
- 2026-03-29 04:49 EDT — Fixed CRM Customer Panel live search so it uses all typed searchable fields together instead of only the last edited field. Files: `components/CRMWorkspace.tsx`, `pos-dashboard-backend/src/routes/crmRoutesV2.ts`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-mAniEY9n.js`. Changes: frontend now rebuilds the live-search query from all populated searchable Customer Panel fields (`first`, `last`, `phone`, `email`, `source`, `interest`, `notes`, etc.) and shows the combined active fields in the search label; backend CRM search now tokenizes the query and requires every token to match across customer/order fields, while still matching normalized phone digits. Commands: frontend `npm run build`, backend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `pm2 restart pos-api`, `curl -s http://127.0.0.1:5057/health`, `grep -n "buildCustomerSearchFromDraft\\|buildLooseSearchClause\\|orderSearch" /home/alphahs/WOLF-FD/components/CRMWorkspace.tsx /home/alphahs/WOLF-FD/pos-dashboard-backend/src/routes/crmRoutesV2.ts`, `date`. Tests: frontend build PASS; backend build PASS; FD health PASS (`{"ok":true,"db":1}`).
- 2026-03-29 04:52 EDT — Removed the duplicate Sales Analysis range label from the sticky app header so the condensed header only shows the editable range control on the right beside the search bar. Files: `App.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-8S2Q4cbe.js`. Changes: deleted the title-side `(range)` badge next to `Sales Analysis` in the sticky header while keeping the existing right-side `Range: ... Edit` control. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `date`. Tests: frontend build PASS; live asset deploy PASS.
- 2026-03-29 04:55 EDT — Corrected the CRM Customer Panel search follow-up so it only combines fields the user actively typed into during the current search, instead of pulling in untouched default draft values like `Showroom Walk-In` and `Follow up`. Files: `components/CRMWorkspace.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-C19Pg4ja.js`. Changes: replaced draft-wide live-search rebuilding with dedicated search-field tracking for typed CRM search fields, while keeping the multi-field combined-name behavior intact. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `grep -n "buildCustomerSearchFromFieldValues\\|customerSearchFieldValues" /home/alphahs/WOLF-FD/components/CRMWorkspace.tsx`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `date`. Tests: frontend build PASS; live asset deploy PASS.
- 2026-03-29 04:58 EDT — Adjusted CRM Customer Panel result ordering so broad searches float matching orders to the top when there is not enough search detail to confidently narrow to a single customer. Files: `components/CRMWorkspace.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-C2xjDl64.js`. Changes: added a broad-search heuristic (`one field or one token`) and, when matching orders exist, order results now appear before saved-customer results in the top-10 live matches list. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `grep -n "shouldPrioritizeOrders\\|queryTokens" /home/alphahs/WOLF-FD/components/CRMWorkspace.tsx`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `date`. Tests: frontend build PASS; live asset deploy PASS.
- 2026-03-29 05:08 EDT — Removed the `Coming Soon` placeholder hero from the Tasks page so it opens directly into the live task board. Files: `components/TaskManager.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-D_Wjk5Ep.js`. Changes: deleted the amber placeholder section at the top of `TaskManager` and left the working task board header/controls as the first visible content. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `grep -n "Coming Soon\\|Task board improvements are coming soon" /home/alphahs/WOLF-FD/components/TaskManager.tsx`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `date`. Tests: frontend build PASS; placeholder-string grep returned no matches.
- 2026-03-29 05:19 EDT — Planned the next-phase Social Posts architecture to move provider credentials and API configuration into Settings so future business integrations can be managed centrally instead of being hardcoded inside the Social Posts module. Files reviewed: `components/WorkAdvertising.tsx`, `components/OwnerSettings.tsx`, `services/socialApi.ts`, `pos-dashboard-backend/src/routes/socialRoutes.ts`, `pos-dashboard-backend/src/socialPublishing.ts`, `pos-dashboard-backend/src/startupBootstrap.ts`, `pos-dashboard-backend/src/server.ts`, `AGENTS.md`. Decisions: keep existing social asset/post/job backend, add a `Social Integrations` settings panel for Facebook/Instagram/Google connection data, simplify Social Posts into compose/draft/schedule/publish workflow, and treat `Save Draft` versus `Schedule` as separate states. Commands: `grep -RIn "Social Posts\\|settings\\|token\\|registerSocialRoutes\\|runSocialDueJobsOnce" ...`, multiple `sed -n` code inspections, `date`. Tests: not run; planning only.
- 2026-03-29 05:26 EDT — Implemented the first Social Posts integration-management slice by moving platform connection editing into Settings and replacing the embedded Social Posts auth editor with owner-only shortcut/status UX. Files: `components/settings/SocialIntegrationsSettings.tsx`, `components/OwnerSettings.tsx`, `components/WorkAdvertising.tsx`, `App.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-C9-G2cxo.js`. Changes: added a new `Social Integrations` settings panel backed by existing `social_accounts` APIs for Facebook/Instagram/Google connection data, wired `OwnerSettings` to open requested panels programmatically, updated `App.tsx` so Social Posts can jump owners directly into that settings panel, removed the large in-module platform-connections editor from Social Posts, and added connection-status cards plus an owner-only `Open Integrations`/missing-setup shortcut. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -n "Social Integrations\\|Open Integrations\\|Connection Status" /home/alphahs/WOLF-FD/components/settings/SocialIntegrationsSettings.tsx /home/alphahs/WOLF-FD/components/OwnerSettings.tsx /home/alphahs/WOLF-FD/components/WorkAdvertising.tsx /home/alphahs/WOLF-FD/App.tsx`, `date`. Tests: frontend build PASS; live asset deploy PASS.
- 2026-03-29 05:37 EDT — Tightened the Social Posts integrations flow with platform-aware readiness checks based on current official Meta and Google docs. Files: `components/workAdvertising/socialPlatformGuidance.ts`, `components/WorkAdvertising.tsx`, `components/settings/SocialIntegrationsSettings.tsx`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-DHMyIZFe.js`. Changes: added shared Facebook/Instagram/Google platform guidance metadata, token-expiry evaluation, and readiness checks; expanded Social Integrations settings with doc links, per-platform requirement notes, token-health feedback, and extra config JSON storage; added Social Posts composer `Readiness Check` panels that block schedule/publish on missing setup or incompatible assets while keeping drafts saveable; surfaced current doc-aligned notes for Instagram PPA, Google `business.manage`, refresh-token recommendations, and March 24 2025 Instagram `alt_text` support as a future enhancement note. Commands: official-doc review via `curl -L -A 'Mozilla/5.0' -s https://developers.facebook.com/docs/pages-api/posts`, `curl -L -A 'Mozilla/5.0' -s https://developers.facebook.com/docs/instagram-platform/content-publishing`, `curl -L -s https://developers.google.com/my-business/content/posts-data`, `curl -L -s https://developers.google.com/my-business/content/implement-oauth`; frontend `npm run build`; `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`; `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`; `grep -RIn "Readiness Check\\|Configure Missing APIs\\|Page Publishing Authorization\\|business.manage" /srv/www/wolf.discount/fd/assets /srv/www/wolf.discount/fd/index.html | head -n 20`; `date`. Tests: frontend build PASS; live asset deploy PASS; live bundle string verification PASS.
- 2026-03-29 05:45 EDT — Fixed the Message Board `/api/board/messages` mapper crash caused by mutation handlers assuming the insert/update query always returned a full joined row. File: `pos-dashboard-backend/src/routes/boardRoutes.ts`; backend service: `pos-api`. Changes: made `mapMessageRow` null-safe, added `ensureMappedMessageRow`, and changed board create/edit/forward handlers to `RETURNING id` first and then reload the saved message with `loadMessageForAccess` before mapping it. Commands: backend `npm run build` in `/home/alphahs/WOLF-FD/pos-dashboard-backend`, `pm2 restart pos-api`, `curl -s http://127.0.0.1:5057/health`, `curl -i -s http://127.0.0.1:5057/api/board/messages`, `grep -n "ensureMappedMessageRow\\|loadMessageForAccess" /home/alphahs/WOLF-FD/pos-dashboard-backend/src/routes/boardRoutes.ts`, `date`. Tests: backend build PASS; FD health PASS (`{"ok":true,"db":1}`); board route probe now returns JSON `401 unauthorized` without crashing.
- 2026-03-29 05:50 EDT — Restored a detailed print-only Sales Analysis layout while keeping the live on-screen report compact. Files: `components/sales/SalesCorePrintSections.tsx`, `components/sales/SalesDrilldownPrintSections.tsx`, `components/app/themeStyles.ts`, `AGENTS.md`; deployed asset: `/srv/www/wolf.discount/fd/assets/index-BO9LSuc1.js`. Changes: expanded print-only store/salesperson/drilldown tables to include tickets, average ticket, Pro1st %, separate sales and unit percentage columns, and margin; removed print-table fixed-width squeezing; and set print pages to landscape with tighter print-table cell spacing so the detailed report fits on paper again. Commands: frontend `npm run build`, `cp -r /home/alphahs/WOLF-FD/dist/* /srv/www/wolf.discount/fd/`, `ls -lt /srv/www/wolf.discount/fd/assets | head -n 5`, `grep -RIn "fd-print-detailed-table\\|Avg Ticket\\|Sales % View\\|Tickets" /srv/www/wolf.discount/fd/assets /srv/www/wolf.discount/fd/index.html | head -n 20`, `date`. Tests: frontend build PASS; live asset deploy PASS.
